{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "buildCommand": "python scripts/reindex.py"
  },
  "deploy": {
    "startCommand": "uvicorn app.main:app --host 0.0.0.0 --port $PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
